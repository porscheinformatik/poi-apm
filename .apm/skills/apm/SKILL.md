---
name: apm
description: Use when writing, modifying, reviewing, or optimizing APM primitives and package configuration under .apm/ and apm.yml.
---

This skill focuses on authoring APM packages: primitives, manifests, and configuration.

## Quick navigation

- **Authoring APM packages**: Author source primitives under `.apm/` and `apm.yml`
- **Authoring validation loop**: Use `compile`, `install --dry-run`, `preview`, and `audit` to verify authored content

## Related prompt workflows

- **`apm-skill` prompt**: Create or update one APM skill from documentation and code references
- **`apm-prompt` prompt**: Create or update one reusable APM prompt from chat or instructions
- **`apm-instructions` prompt**: Create or update one APM instructions file from source files
- **`apm-agent` prompt**: Create or update one APM agent workflow from requirements and step definitions
- **`review-apm` prompt**: Review `.apm` files for concision, validity, and portability
- **`optimize-apm` prompt**: Tighten `.apm` files while preserving intent and behavior
- **`apm-summary` prompt**: Create or update the repository APM summary from local `.apm` content
- **`setup-apm` prompt**: Install, check, repair, or update the APM application itself

LOAD references/authoring.md
